{
    "name": "tsgo-playground",
    "private": true,
    "type": "module",
    "scripts": {
        "wasm": "cd .. && GOOS=js GOARCH=wasm go build -o playground/public/tsgo.wasm ./cmd/tsgo && cp $(go env GOROOT)/lib/wasm/wasm_exec.js playground/public/wasm_exec.js",
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview"
    },
    "dependencies": {
        "monaco-editor": "^0.55.1"
    },
    "devDependencies": {
        "typescript": "^6.0.3",
        "vite": "^8.0.12"
    }
}
