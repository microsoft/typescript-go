// Code generated by convertFourslash; DO NOT EDIT.
// To modify this test, run "npm run makemanual codeFixUnusedIdentifier_destructure_partlyUnused"

package fourslash_test

import (
	"testing"

	"github.com/microsoft/typescript-go/internal/fourslash"
	"github.com/microsoft/typescript-go/internal/testutil"
)

func TestCodeFixUnusedIdentifier_destructure_partlyUnused(t *testing.T) {
	fourslash.SkipIfFailing(t)
	t.Parallel()
	defer testutil.RecoverAndFail(t, "Panic on fourslash test")
	const content = `// @noUnusedLocals: true
{
    const { x, y } = o;
    x;
}
{
    const { x, y } = o;
    y;
}
{
    const { x, y, z } = o;
    y;
}
{
    const { x, y, z } = o;
    x; z;
}
{
    const [x, y] = o;
    x;
}
{
    const [x, y] = o;
    y;
}
{
    const [x, y, z] = o;
    y;
}
{
    const [x, y, z] = o;
    x; z;
}`
	f, done := fourslash.NewFourslash(t, nil /*capabilities*/, content)
	defer done()
	f.VerifyCodeFixAll(t, fourslash.VerifyCodeFixAllOptions{
		FixID: "unusedIdentifier_delete",
		NewFileContent: `{
    const { x } = o;
    x;
}
{
    const { y } = o;
    y;
}
{
    const { y } = o;
    y;
}
{
    const { x, z } = o;
    x; z;
}
{
    const [x, y] = o;
    x;
}
{
    const [x, y] = o;
    y;
}
{
    const [x, y, z] = o;
    y;
}
{
    const [x, y, z] = o;
    x; z;
}`,
	})
}
