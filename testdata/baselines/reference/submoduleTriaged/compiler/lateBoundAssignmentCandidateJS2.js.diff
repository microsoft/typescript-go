--- old.lateBoundAssignmentCandidateJS2.js
+++ new.lateBoundAssignmentCandidateJS2.js
@@= skipped -30, +30 lines =@@


 //// [index.d.ts]
+const prop = "prop";
 export class foo1 {
+    constructor();
     /**
      * @protected
      * @type {string}
      */
-    protected prop: string;
+    protected [prop]: string;
 }
+export {};
+
+
+//// [DtsFileErrors]
+
+
+dist/index.d.ts(1,1): error TS1046: Top-level declarations in .d.ts files must start with either a 'declare' or 'export' modifier.
+
+
+==== dist/index.d.ts (1 errors) ====
+    const prop = "prop";
+    ~~~~~
+!!! error TS1046: Top-level declarations in .d.ts files must start with either a 'declare' or 'export' modifier.
+    export class foo1 {
+        constructor();
+        /**
+         * @protected
+         * @type {string}
+         */
+        protected [prop]: string;
+    }
+    export {};
+    