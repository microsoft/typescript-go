--- old.typePredicateInLoop.errors.txt
+++ new.typePredicateInLoop.errors.txt
@@= skipped -18, +18 lines =@@
       if (guard(arg)) {
         for (const ITEM of arg.arr) {
           if (otherFunc(ITEM, arg)) {
-              ~~~~~~~~~~~~~~~~~~~~
+              ~~~~~~~~~
 !!! error TS1345: An expression of type 'void' cannot be tested for truthiness.
           }
         }
