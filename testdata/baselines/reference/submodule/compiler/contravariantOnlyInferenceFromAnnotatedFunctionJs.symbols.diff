--- old.contravariantOnlyInferenceFromAnnotatedFunctionJs.symbols
+++ new.contravariantOnlyInferenceFromAnnotatedFunctionJs.symbols
@@= skipped -2, +2 lines =@@
 === index.js ===
 /**
  * @typedef {{ [K in keyof B]: { fn: (a: A, b: B) => void; thing: B[K]; } }} Funcs
+>Funcs : Symbol(Funcs, Decl(index.js, 1, 3))
+
  * @template A
+>A : Symbol(A, Decl(index.js, 2, 13))
+
  * @template {Record<string, unknown>} B
+>B : Symbol(B, Decl(index.js, 3, 39))
+>Record : Symbol(Record, Decl(lib.es5.d.ts, --, --))
+
  */
 
 /**
