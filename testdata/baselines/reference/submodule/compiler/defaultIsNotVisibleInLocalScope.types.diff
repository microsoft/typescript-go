--- old.defaultIsNotVisibleInLocalScope.types
+++ new.defaultIsNotVisibleInLocalScope.types
@@= skipped -3, +3 lines =@@
 export default function () {
     return true;
 >true : true
->     : ^^^^
 }
 export type X = typeof default;  // expect error
 >X : any
->  : ^^^
->default : any
->        : ^^^
 
 === b.ts ===
 export default { a: true }
 >{ a: true } : { a: boolean; }
->            : ^^^^^^^^^^^^^^^
 >a : boolean
->  : ^^^^^^^
 >true : true
->     : ^^^^
 
 export type X = typeof default; // expect error
 >X : any
->  : ^^^
->default : any
->        : ^^^
 
