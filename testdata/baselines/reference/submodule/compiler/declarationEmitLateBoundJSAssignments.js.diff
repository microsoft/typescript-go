--- old.declarationEmitLateBoundJSAssignments.js
+++ new.declarationEmitLateBoundJSAssignments.js
@@= skipped -21, +21 lines =@@
 const a = foo[dashStrMem];
 
 
-
-
-//// [file.d.ts]
-export function foo(): void;
-export namespace foo {
-    let bar: number;
-    let strMemName: string;
-}
+//// [file.js]
+export function foo() { }
+foo.bar = 12;
+const _private = Symbol();
+foo[_private] = "ok";
+const strMem = "strMemName";
+foo[strMem] = "ok";
+const dashStrMem = "dashed-str-mem";
+foo[dashStrMem] = "ok";
+const numMem = 42;
+foo[numMem] = "ok";
+const x = foo[_private];
+const y = foo[strMem];
+const z = foo[numMem];
+const a = foo[dashStrMem];
