--- old.controlFlowJavascript.js
+++ new.controlFlowJavascript.js
@@= skipped -105, +105 lines =@@
 }
 
 
-//// [out.js]
-var cond = true;
-// CFA for 'let' and no initializer
+//// [controlFlowJavascript.js]
+let cond = true;
 function f1() {
-    var x;
+    let x;
     if (cond) {
         x = 1;
     }
     if (cond) {
         x = "hello";
     }
-    var y = x; // string | number | undefined
+    const y = x;
 }
-// CFA for 'let' and 'undefined' initializer
 function f2() {
-    var x = undefined;
+    let x = undefined;
     if (cond) {
         x = 1;
     }
     if (cond) {
         x = "hello";
     }
-    var y = x; // string | number | undefined
+    const y = x;
 }
-// CFA for 'let' and 'null' initializer
 function f3() {
-    var x = null;
+    let x = null;
     if (cond) {
         x = 1;
     }
     if (cond) {
         x = "hello";
     }
-    var y = x; // string | number | null
+    const y = x;
 }
-// CFA for 'var' with no initializer
 function f5() {
     var x;
     if (cond) {
@@= skipped -44, +40 lines =@@
     if (cond) {
         x = "hello";
     }
-    var y = x; // string | number | undefined
+    const y = x;
 }
-// CFA for 'var' with 'undefined' initializer
 function f6() {
     var x = undefined;
     if (cond) {
@@= skipped -11, +10 lines =@@
     if (cond) {
         x = "hello";
     }
-    var y = x; // string | number | undefined
+    const y = x;
 }
-// CFA for 'var' with 'null' initializer
 function f7() {
     var x = null;
     if (cond) {
@@= skipped -11, +10 lines =@@
     if (cond) {
         x = "hello";
     }
-    var y = x; // string | number | null
+    const y = x;
 }
-// No CFA for captured outer variables
 function f9() {
-    var x;
+    let x;
     if (cond) {
         x = 1;
     }
     if (cond) {
         x = "hello";
     }
-    var y = x; // string | number | undefined
+    const y = x;
     function f() {
-        var z = x; // any
+        const z = x;
     }
 }
-// No CFA for captured outer variables
 function f10() {
-    var x;
+    let x;
     if (cond) {
         x = 1;
     }
     if (cond) {
         x = "hello";
     }
-    var y = x; // string | number | undefined
-    var f = function () {
-        var z = x; // any
+    const y = x;
+    const f = () => {
+        const z = x;
     };
 }
