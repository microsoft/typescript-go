--- old.recursiveGenericMethodCall.types
+++ new.recursiveGenericMethodCall.types
@@= skipped -4, +4 lines =@@

 function Generate<T>(func: Generator<T>): T {
 >Generate : <T>(func: Generator<T>) => T
->func : Generator<T, any, any>
+>func : Generator<T>

     return Generate(func);
 >Generate(func) : T
 >Generate : <T>(func: Generator<T>) => T
->func : Generator<T, any, any>
+>func : Generator<T>
 }
