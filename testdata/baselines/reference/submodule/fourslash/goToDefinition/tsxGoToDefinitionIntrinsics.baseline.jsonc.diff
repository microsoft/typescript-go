--- old.tsxGoToDefinitionIntrinsics.baseline.jsonc
+++ new.tsxGoToDefinitionIntrinsics.baseline.jsonc
@@= skipped -13, +13 lines =@@
 // var y = <span />;
 // var z = <div name='hello' />;

+
+
 // === goToDefinition ===
 // === /file.tsx ===
 // --- (line: 4) skipped ---
@@= skipped -12, +14 lines =@@
 // var x = <div />;
 // var y = <s/*GOTO DEF*/pan />;
 // var z = <div name='hello' />;
+
+

 // === goToDefinition ===
 // === /file.tsx ===