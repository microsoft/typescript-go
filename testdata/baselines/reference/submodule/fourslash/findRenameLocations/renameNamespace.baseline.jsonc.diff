--- old.renameNamespace.baseline.jsonc
+++ new.renameNamespace.baseline.jsonc
@@= skipped --1, +0 lines =@@
+// === findRenameLocations ===
+// === /renameNamespace.ts ===
+// namespace /*RENAME*/[|NSRENAME|] {
+//     export const enum E {
+//         A = 'a'
+//     }
+// }
+// 
+// const a: [|NSRENAME|].E = [|NSRENAME|].E.A;