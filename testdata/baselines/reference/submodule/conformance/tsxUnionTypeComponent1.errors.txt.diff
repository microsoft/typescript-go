--- old.tsxUnionTypeComponent1.errors.txt
+++ new.tsxUnionTypeComponent1.errors.txt
@@= skipped -0, +0 lines =@@
-<no content>
+error TS-1: Pre-emit (0) and post-emit (2) diagnostic counts do not match! This can indicate that a semantic _error_ was added by the emit resolver - such an error may not be reflected on the command line or in the editor, but may be captured in a baseline here!
+
+
+!!! error TS-1: Pre-emit (0) and post-emit (2) diagnostic counts do not match! This can indicate that a semantic _error_ was added by the emit resolver - such an error may not be reflected on the command line or in the editor, but may be captured in a baseline here!
+!!! related TS-1: The excess diagnostics are:
+!!! related TS2304 file.tsx:17:35: Cannot find name 'button'.
+!!! related TS2304 file.tsx:17:48: Cannot find name 'button'.
+==== file.tsx (0 errors) ====
+    /// <reference path="/.lib/react.d.ts" />
+    
+    import React = require('react');
+    
+    interface ComponentProps {
+        AnyComponent: React.StatelessComponent<any> | React.ComponentClass<any>;
+    }
+    
+    class MyComponent extends React.Component<ComponentProps, {}> {
+        render() {
+            const { AnyComponent } = this.props;
+            return (<AnyComponent />);
+        }
+    }
+    
+    // Stateless Component As Props
+    <MyComponent AnyComponent={() => <button>test</button>}/>
+    
+    // Component Class as Props
+    class MyButtonComponent extends React.Component<{},{}> {
+    }
+    
+    <MyComponent AnyComponent={MyButtonComponent} />
+    
+    