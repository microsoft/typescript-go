--- old.topLevelAwaitErrors.2(module=esnext).types
+++ new.topLevelAwaitErrors.2(module=esnext).types
@@= skipped -5, +5 lines =@@
 // reparse variable name as await should fail
 var await = 1;
 >await : number
->      : ^^^^^^
 >1 : 1
->  : ^
 
