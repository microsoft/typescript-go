--- old.tsxGenericAttributesType3.errors.txt
+++ new.tsxGenericAttributesType3.errors.txt
@@= skipped -0, +0 lines =@@
-<no content>
+error TS-1: Pre-emit (0) and post-emit (2) diagnostic counts do not match! This can indicate that a semantic _error_ was added by the emit resolver - such an error may not be reflected on the command line or in the editor, but may be captured in a baseline here!
+
+
+!!! error TS-1: Pre-emit (0) and post-emit (2) diagnostic counts do not match! This can indicate that a semantic _error_ was added by the emit resolver - such an error may not be reflected on the command line or in the editor, but may be captured in a baseline here!
+!!! related TS-1: The excess diagnostics are:
+!!! related TS2304 file.tsx:7:17: Cannot find name 'div'.
+!!! related TS2304 file.tsx:7:25: Cannot find name 'div'.
+==== file.tsx (0 errors) ====
+    /// <reference path="/.lib/react.d.ts" />
+    
+    import React = require('react');
+    
+    class B1<T extends { x: string } = { x:string } > extends React.Component<T, {}> {
+        render() {
+            return <div>hi</div>; 
+        }
+    }
+    class B<U> extends React.Component<U, {}> {
+        render() {
+            return <B1 {...this.props} x="hi" />;
+        }
+    }