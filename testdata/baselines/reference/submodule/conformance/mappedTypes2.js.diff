--- old.mappedTypes2.js
+++ new.mappedTypes2.js
@@= skipped -140, +140 lines =@@


 //// [mappedTypes2.d.ts]
-declare function verifyLibTypes<T, K extends keyof T, U>(): void;
+function verifyLibTypes<T, K extends keyof T, U>(): void;
 type Proxy<T> = {
     get(): T;
     set(value: T): void;
@@= skipped -11, +11 lines =@@
 type DeepReadonly<T> = {
     readonly [P in keyof T]: DeepReadonly<T[P]>;
 };
-declare function assign<T>(obj: T, props: Partial<T>): void;
-declare function freeze<T>(obj: T): Readonly<T>;
-declare function pick<T, K extends keyof T>(obj: T, ...keys: K[]): Pick<T, K>;
-declare function mapObject<K extends string, T, U>(obj: Record<K, T>, f: (x: T) => U): Record<K, U>;
-declare function proxify<T>(obj: T): Proxify<T>;
+function assign<T>(obj: T, props: Partial<T>): void;
+function freeze<T>(obj: T): Readonly<T>;
+function pick<T, K extends keyof T>(obj: T, ...keys: K[]): Pick<T, K>;
+function mapObject<K extends string, T, U>(obj: Record<K, T>, f: (x: T) => U): Record<K, U>;
+function proxify<T>(obj: T): Proxify<T>;
 interface Point {
     x: number;
     y: number;
@@= skipped -27, +27 lines =@@
     readonly height: number;
     readonly location: Point;
 }
-declare function f0(s1: Shape, s2: Shape): void;
-declare function f1(shape: Shape): void;
-declare function f2(shape: Shape): void;
-declare function f3(shape: Shape): void;
-declare function f4(): void;
-declare function f5(shape: Shape): void;
-declare function f6(shape: DeepReadonly<Shape>): void;
+function f0(s1: Shape, s2: Shape): void;
+function f1(shape: Shape): void;
+function f2(shape: Shape): void;
+function f3(shape: Shape): void;
+function f4(): void;
+function f5(shape: Shape): void;
+function f6(shape: DeepReadonly<Shape>): void;
+
+
+//// [DtsFileErrors]
+
+
+mappedTypes2.d.ts(1,1): error TS1046: Top-level declarations in .d.ts files must start with either a 'declare' or 'export' modifier.
+
+
+==== mappedTypes2.d.ts (1 errors) ====
+    function verifyLibTypes<T, K extends keyof T, U>(): void;
+    ~~~~~~~~
+!!! error TS1046: Top-level declarations in .d.ts files must start with either a 'declare' or 'export' modifier.
+    type Proxy<T> = {
+        get(): T;
+        set(value: T): void;
+    };
+    type Proxify<T> = {
+        [P in keyof T]: Proxy<T[P]>;
+    };
+    type DeepReadonly<T> = {
+        readonly [P in keyof T]: DeepReadonly<T[P]>;
+    };
+    function assign<T>(obj: T, props: Partial<T>): void;
+    function freeze<T>(obj: T): Readonly<T>;
+    function pick<T, K extends keyof T>(obj: T, ...keys: K[]): Pick<T, K>;
+    function mapObject<K extends string, T, U>(obj: Record<K, T>, f: (x: T) => U): Record<K, U>;
+    function proxify<T>(obj: T): Proxify<T>;
+    interface Point {
+        x: number;
+        y: number;
+    }
+    interface Shape {
+        name: string;
+        width: number;
+        height: number;
+        location: Point;
+    }
+    interface PartialShape {
+        name?: string;
+        width?: number;
+        height?: number;
+        location?: Point;
+    }
+    interface ReadonlyShape {
+        readonly name: string;
+        readonly width: number;
+        readonly height: number;
+        readonly location: Point;
+    }
+    function f0(s1: Shape, s2: Shape): void;
+    function f1(shape: Shape): void;
+    function f2(shape: Shape): void;
+    function f3(shape: Shape): void;
+    function f4(): void;
+    function f5(shape: Shape): void;
+    function f6(shape: DeepReadonly<Shape>): void;
+    