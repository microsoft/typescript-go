--- old.typeFromPropertyAssignment37.errors.txt
+++ new.typeFromPropertyAssignment37.errors.txt
@@= skipped -0, +0 lines =@@
-<no content>
+mod.js(1,14): error TS2631: Cannot assign to '"mod"' because it is a namespace.
+
+
+==== use.js (0 errors) ====
+    const util = require('./mod')
+    function n() {
+        util.existy // no error
+    }
+    util.existy // no error
+    
+==== mod.js (1 errors) ====
+    const util = exports = module.exports = {}
+                 ~~~~~~~
+!!! error TS2631: Cannot assign to '"mod"' because it is a namespace.
+    if (!!false) {
+        util.existy = function () { }
+    }
+    