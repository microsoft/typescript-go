--- old.derivedClassSuperStatementPosition.errors.txt
+++ new.derivedClassSuperStatementPosition.errors.txt
@@= skipped -73, +73 lines =@@
         constructor() {
             Math.random()
                 ? super(1)
-                  ~~~~~~~~
+                  ~~~~~
 !!! error TS2401: A 'super' call must be a root-level statement within a constructor of a derived class that contains initialized properties, parameter properties, or private identifiers.
                 : super(0);
         }
@@= skipped -11, +11 lines =@@
         constructor() {
             if (Math.random()) {
                 super(1);
-                ~~~~~~~~
+                ~~~~~
 !!! error TS2401: A 'super' call must be a root-level statement within a constructor of a derived class that contains initialized properties, parameter properties, or private identifiers.
             }
             else {
@@= skipped -14, +14 lines =@@
         constructor(private paramProp = 2) {
             {
                 super();
-                ~~~~~~~
+                ~~~~~
 !!! error TS2401: A 'super' call must be a root-level statement within a constructor of a derived class that contains initialized properties, parameter properties, or private identifiers.
             }
         }
@@= skipped -11, +11 lines =@@
         constructor(private paramProp = 2) {
             if (Math.random()) {
                 super(1);
-                ~~~~~~~~
+                ~~~~~
 !!! error TS2401: A 'super' call must be a root-level statement within a constructor of a derived class that contains initialized properties, parameter properties, or private identifiers.
             } else {
                 super(0);
