--- old.thisTypeInObjectLiterals2(target=es2015).js
+++ new.thisTypeInObjectLiterals2(target=es2015).js
@@= skipped -406, +406 lines =@@


 //// [thisTypeInObjectLiterals2.d.ts]
-declare let obj1: {
+let obj1: {
     a: number;
     f(): number;
     b: string;
@@= skipped -16, +16 lines =@@
     z?: number;
     moveBy(dx: number, dy: number, dz?: number): void;
 };
-declare let p1: Point;
-declare let p2: Point | null;
-declare let p3: Point | undefined;
-declare let p4: Point | null | undefined;
-declare function f1(p: Point): void;
-declare function f2(p: Point | null | undefined): void;
+let p1: Point;
+let p2: Point | null;
+let p3: Point | undefined;
+let p4: Point | null | undefined;
+function f1(p: Point): void;
+function f2(p: Point | null | undefined): void;
 type ObjectDescriptor<D, M> = {
     data?: D;
     methods?: M & ThisType<D & M>;
 };
-declare function makeObject<D, M>(desc: ObjectDescriptor<D, M>): D & M;
-declare let x1: {
+function makeObject<D, M>(desc: ObjectDescriptor<D, M>): D & M;
+let x1: {
     x: number;
     y: number;
 } & {
@@= skipped -21, +21 lines =@@
     data?: D;
     methods?: M;
 };
-declare function makeObject2<D, M>(desc: ObjectDescriptor<D, M>): D & M;
-declare let x2: {
+function makeObject2<D, M>(desc: ObjectDescriptor<D, M>): D & M;
+let x2: {
     x: number;
     y: number;
 } & {
@@= skipped -15, +15 lines =@@
 type PropDescMap<T> = {
     [K in keyof T]: PropDesc<T[K]>;
 };
-declare function defineProp<T, K extends string, U>(obj: T, name: K, desc: PropDesc<U> & ThisType<T>): T & Record<K, U>;
-declare function defineProps<T, U>(obj: T, descs: PropDescMap<U> & ThisType<T>): T & U;
-declare let p10: Point & Record<"foo", number>;
-declare let p11: Point & Record<"bar", number>;
-declare let p12: Point & {
+function defineProp<T, K extends string, U>(obj: T, name: K, desc: PropDesc<U> & ThisType<T>): T & Record<K, U>;
+function defineProps<T, U>(obj: T, descs: PropDescMap<U> & ThisType<T>): T & U;
+let p10: Point & Record<"foo", number>;
+let p11: Point & Record<"bar", number>;
+let p12: Point & {
     foo: number;
     bar: number;
 };
@@= skipped -23, +23 lines =@@
     methods?: M;
     computed?: Accessors<P>;
 };
-declare const Vue: new <D, M, P>(options: VueOptions<D, M, P>) => D & M & P;
-declare let vue: {
+const Vue: new <D, M, P>(options: VueOptions<D, M, P>) => D & M & P;
+let vue: {
     x: number;
     y: number;
 } & {
@@= skipped -10, +10 lines =@@
     test: number;
     hello: string;
 };
+
+
+//// [DtsFileErrors]
+
+
+thisTypeInObjectLiterals2.d.ts(1,1): error TS1046: Top-level declarations in .d.ts files must start with either a 'declare' or 'export' modifier.
+
+
+==== thisTypeInObjectLiterals2.d.ts (1 errors) ====
+    let obj1: {
+    ~~~
+!!! error TS1046: Top-level declarations in .d.ts files must start with either a 'declare' or 'export' modifier.
+        a: number;
+        f(): number;
+        b: string;
+        c: {
+            g(): void;
+        };
+        readonly d: number;
+        e: string;
+    };
+    type Point = {
+        x: number;
+        y: number;
+        z?: number;
+        moveBy(dx: number, dy: number, dz?: number): void;
+    };
+    let p1: Point;
+    let p2: Point | null;
+    let p3: Point | undefined;
+    let p4: Point | null | undefined;
+    function f1(p: Point): void;
+    function f2(p: Point | null | undefined): void;
+    type ObjectDescriptor<D, M> = {
+        data?: D;
+        methods?: M & ThisType<D & M>;
+    };
+    function makeObject<D, M>(desc: ObjectDescriptor<D, M>): D & M;
+    let x1: {
+        x: number;
+        y: number;
+    } & {
+        moveBy(dx: number, dy: number): void;
+    };
+    type ObjectDescriptor2<D, M> = ThisType<D & M> & {
+        data?: D;
+        methods?: M;
+    };
+    function makeObject2<D, M>(desc: ObjectDescriptor<D, M>): D & M;
+    let x2: {
+        x: number;
+        y: number;
+    } & {
+        moveBy(dx: number, dy: number): void;
+    };
+    type PropDesc<T> = {
+        value?: T;
+        get?(): T;
+        set?(value: T): void;
+    };
+    type PropDescMap<T> = {
+        [K in keyof T]: PropDesc<T[K]>;
+    };
+    function defineProp<T, K extends string, U>(obj: T, name: K, desc: PropDesc<U> & ThisType<T>): T & Record<K, U>;
+    function defineProps<T, U>(obj: T, descs: PropDescMap<U> & ThisType<T>): T & U;
+    let p10: Point & Record<"foo", number>;
+    let p11: Point & Record<"bar", number>;
+    let p12: Point & {
+        foo: number;
+        bar: number;
+    };
+    type Accessors<T> = {
+        [K in keyof T]: (() => T[K]) | Computed<T[K]>;
+    };
+    type Dictionary<T> = {
+        [x: string]: T;
+    };
+    type Computed<T> = {
+        get?(): T;
+        set?(value: T): void;
+    };
+    type VueOptions<D, M, P> = ThisType<D & M & P> & {
+        data?: D | (() => D);
+        methods?: M;
+        computed?: Accessors<P>;
+    };
+    const Vue: new <D, M, P>(options: VueOptions<D, M, P>) => D & M & P;
+    let vue: {
+        x: number;
+        y: number;
+    } & {
+        f(x: string): number;
+    } & {
+        test: number;
+        hello: string;
+    };
+    