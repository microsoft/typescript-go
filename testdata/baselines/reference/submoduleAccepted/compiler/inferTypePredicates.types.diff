--- old.inferTypePredicates.types
+++ new.inferTypePredicates.types
@@= skipped -547, +547 lines =@@
 } else {
   let t: object = maybeDate;  // should ok
 >t : object
->maybeDate : object
+>maybeDate : object & not Date
 }

 if (flakyIsDate(maybeDate)) {
@@= skipped -327, +327 lines =@@
 >"number" : "number"
 >typeof x === "string" : boolean
 >typeof x : "bigint" | "boolean" | "function" | "number" | "object" | "string" | "symbol" | "undefined"
->x : unknown
+>x : not number
 >"string" : "string"
 }
 declare let unk: unknown;