{
    "private": true,
    "name": "scripts",
    "version": "1.0.0",
    "description": "Test conversion script",
    "scripts": {
        "build": "npx tsc -p .",
        "runjs": "node dist/convertFourslash.js",
        "run": "npx tsc -p . && node dist/convertFourslash.js"
    }
}
