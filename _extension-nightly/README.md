# TypeScript 7 Nightly

This extension contains the nightly build of TypeScript for the TypeScript extension.

If installed, this extension's version of TypeScript will be used by the TypeScript extension.
